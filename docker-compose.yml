services:
  ts3-manager:
    build:
      context: .
    image: ts3-manager-custom:latest
    container_name: ts3-manager
    restart: unless-stopped
    ports:
      - "${WEB_PORT:-8080}:8080"
    environment:
      PORT: "8080"
      JWT_SECRET: "${JWT_SECRET}"
      WHITELIST: "${WHITELIST:-}"
